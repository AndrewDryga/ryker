# Owner-only environment for the Elixir/PostgreSQL service. Replace every value
# before start. This file carries deployment connections and credentials only:
# product settings live in PostgreSQL and are changed through the console.
DATABASE_URL=ecto://ryker:replace-me@127.0.0.1/ryker
POOL_SIZE=20

# Base64 for exactly 32 random bytes. It encrypts durable remote-work
# checkpoints. Import the existing value on an upgrade; a new key orphans every
# stored workspace checkpoint.
RYKER_CHECKPOINT_KEY=replace-with-base64-32-byte-key
RYKER_CREDENTIAL_KEY=replace-with-base64-32-byte-key
RYKER_STATE_TOOLS_TOKEN=replace-with-state-tools-root-secret

# Outbound worker gateway. Supply all five or none; RYKER_WORKER_IP and
# RYKER_WORKER_PORT (default 127.0.0.1:4322) may only be set beside them.
RYKER_WORKER_PUBLIC_URL=https://ryker.example.com:4322
RYKER_WORKER_CA_FILE=/etc/ryker/worker-ca.pem
RYKER_WORKER_CA_KEY_FILE=/etc/ryker/worker-ca-key.pem
RYKER_WORKER_CERT_FILE=/etc/ryker/worker-gateway.pem
RYKER_WORKER_KEY_FILE=/etc/ryker/worker-gateway-key.pem

# Optional, all with shipped defaults:
# RYKER_CONTROL_IP, RYKER_CONTROL_PORT, RYKER_STATE_DIR,
# RYKER_STATE_TOOLS_IP, RYKER_STATE_TOOLS_PORT,
# RYKER_GITHUB_IP, RYKER_GITHUB_PORT,
# RYKER_WEBHOOK_IP, RYKER_WEBHOOK_PORT,
# LOG_LEVEL.

# Integration credentials are entered and verified in Ryker's guided setup.
# They are encrypted with RYKER_CREDENTIAL_KEY and are never runtime variables.
